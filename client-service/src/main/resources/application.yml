spring:
  application:
    name: client-service
  datasource:
    url: jdbc:postgresql://localhost:5432/bd_client
    username: postgres
    password: kmaskita
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8001
